# Per-device NixOS files. Generate/create these locally; do not commit them.
/hardware-configuration.nix
/host-configuration.nix

# Credentials never belong in this repository.
/ssh-key*
/id_*
/.ssh/
